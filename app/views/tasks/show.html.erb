<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<%= link_to "All Tasks", tasks_path %>

<%= link_to "Edit Task", edittask_path %>

<%= link_to "Delete",
  task_path(@task),
  data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }
%>
